services:
  web:
    image: first-app:latest
    container_name: first-app
    build:
      context: .
    ports:
      - "5005:5005"
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: first-redis
    ports:
      - "6379:6379"